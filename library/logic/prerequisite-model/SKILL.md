---
name: prerequisite-model
description: Before hunting business logic, ensure: 1. Accounts: User A + User B created with test data 2. Scope: Which features are in-scope? (payment, auth, billing) 3. Tools: curl, jq, Burp/CA, proxy list ready 4. App Model: Built via application-...
category: logic
---

# PREREQUISITE MODEL

Before hunting business logic, ensure:
1. Accounts: User A + User B created with test data
2. Scope: Which features are in-scope? (payment, auth, billing)
3. Tools: curl, jq, Burp/CA, proxy list ready
4. App Model: Built via application-model-builder skill
5. Archetype: Loaded via application-archetypes skill
6. Priority: Score each asset's opportunity (high-value, high-confidence first)
7. Reference: SKILL.md execution matrix loaded
